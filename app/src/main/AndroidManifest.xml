<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ammar.buxapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BuxApp">
        <activity android:name=".Soon"></activity>
        <activity android:name=".LoginApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TicketDetail" />
        <activity android:name=".ticket_item" />
        <activity android:name=".BusDetail" />
        <activity android:name=".DatePicker" />
        <activity android:name=".Ratings" />
        <activity android:name=".DestinationAdapter" />
        <activity android:name=".ScheduleList" />
        <activity android:name=".TicketList" />
        <activity android:name=".MainActivity1" />
        <activity android:name=".SaveNumber" />
        <activity android:name=".FinishRegist" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>